# PythonRecursiveTree

This project is a Pygame visualization of a recursively generated binary tree-like structure. It draws a growing branching pattern where each node splits into two new branches, creating a fractal-style tree.
